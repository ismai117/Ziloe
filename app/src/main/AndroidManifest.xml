<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.brand.ziloe">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ziloe">
        <activity
            android:name=".view.activites.PopUp_Activity"
            android:theme="@style/Theme.Ziloe.PopMe" />
        <activity android:name=".view.activites.Confirmation_Activity" />
        <activity android:name=".view.activites.Payment_Activity" />
        <activity android:name=".view.activites.Shipping_Activity" />
        <activity android:name=".view.activites.Information_Activity" />
        <activity android:name=".view.activites.CheckOut_Activity" />
        <activity
            android:name=".view.activites.Cart_Activity"
            android:parentActivityName=".view.activites.MainActivity" />
        <activity android:name=".view.activites.SplashScreen_Activity" android:label="@string/title_activity_splash_screen" android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.activites.MainActivity"/>
    </application>

</manifest>